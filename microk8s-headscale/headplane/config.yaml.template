server:
  host: 0.0.0.0
  port: 3000
  cookie_secret: ${HEADPLANE_COOKIE_SECRET}
  cookie_secure: true
headscale:
  url: http://headscale.${HEADSCALE_NAMESPACE}.svc.cluster.local:8080
  api_key: ${HEADSCALE_API_KEY}
  config_strict: false
